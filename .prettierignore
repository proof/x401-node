dist

# Verbatim spec cache + extracted fixtures — must match upstream byte-for-byte,
# so the conformance harness checks against spec text, not a reformatted copy.
# Regenerated by scripts/sync-spec-fixtures.ts.
spec/spec.md
spec/fixtures/
spec/normative-ledger.json
